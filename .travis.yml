language: java
jdk:
  - oraclejdk8

dist: trusty

cache:
  directories:
  - $HOME/protobuf-3.6.1

install:
  - bash build/install-protobuf.sh
  - mvn dependency:go-offline

script:
  - mvn clean install -Dgpg.skip=true