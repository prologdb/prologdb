package com.github.prologdb.storage.fact

/**
 * An identifier for individual facts stored in a [FactStore].
 * One such identifier is only meaningful to the [FactStore]
 * that defined it.
 */
typealias PersistenceID = Long